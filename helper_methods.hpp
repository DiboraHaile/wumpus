#include <iostream>
#include <string>


class helper_methods{
    public:
    bool Not(bool a);

    bool And(bool a, bool b);

    bool Or(bool a, bool b);

    bool Imp(bool a, bool b);

    bool Bimp(bool a, bool b);
};
